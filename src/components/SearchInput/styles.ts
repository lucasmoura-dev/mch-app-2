import styled from 'styled-components/native';

import { Searchbar } from 'react-native-paper';

export const RoundInput = styled(Searchbar)<any>`
    border-radius: 24px;
    height: 48px;
    elevation: 0;
    border-width: 2px;
    border-color: #CECECE;
`;

